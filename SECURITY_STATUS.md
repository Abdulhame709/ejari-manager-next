# حالة الأمان — EJARI Manager Next

هذا المستودع مستقل عن `suite-manager` ويعمل حالياً على فرع `development`. تم نقل الكود من commit `98359c0142bd25d5c4b126b8006a687cf1530941` دون ملفات `.env` حقيقية أو مفاتيح خدمة.

## ما تم تنفيذه

تم نقل اعتماد طلب الدفع من سلسلة عمليات منفصلة في المتصفح إلى الدالة `public.approve_payment_request(UUID)`. الدالة تتحقق من صلاحية الموظف، تقفل طلب الدفع والفواتير، تنشئ سند القبض وتفاصيله، تحدث الفواتير، ثم تعتمد الطلب وتسجل الحدث في `audit_log` ضمن transaction واحدة. كما تستخدم قفلاً استشارياً لترقيم السندات داخل الشهر وتمنع إعادة اعتماد الطلب بعد تغير حالته.

تم تحديث الواجهة لاستدعاء RPC بدلاً من تعديل الفواتير والسندات مباشرة، وتمت إضافة توقيع الدالة إلى أنواع Supabase. كما أصبح Error Boundary يعرض التفاصيل الفنية في التطوير فقط ولا يكشف رسالة الخطأ الداخلية في الإنتاج.

تم تحديث بعض أدوات البناء إلى إصدارات أحدث متوافقة ضمن السلاسل الحالية، مع نجاح `npm run lint` و`npx tsc --noEmit` و`npm run build` بعد التحديث.

## ما لم يتم تنفيذه بعد

لم تُطبق migration `20260815010000_approve_payment_request_transaction.sql` على Supabase، ولم يتم Resume أو Restore أو تشغيل SQL Editor. يجب تطبيقها فقط بعد استئناف المشروع ومراجعة سجل migrations ووجود الجداول والدوال المطلوبة.

نتيجة `npm audit` الحالية لا تحتوي على ثغرة حرجة، لكنها ما زالت تعرض ثغرات عالية في اعتماديات متداخلة مثل `postcss`, `nanoid`, `js-yaml` و`undici`. لم يتم استخدام `npm audit fix --force` لأن npm يقترح تغييرات كاسرة في سلسلة Nitro. يجب متابعة تحديث هذه الاعتماديات عندما تتوفر إصدارات متوافقة، مع إعادة تشغيل البناء بعد كل تحديث.

## تطبيق migration بأمان

قبل التطبيق، خذ نسخة احتياطية إضافية من قاعدة البيانات. تحقق من أن migrations السابقة الخاصة بـ `payment_requests`, `payment_request_invoices`, `receipts`, `receipt_details`, `can_manage` و`audit_log` مطبقة. بعد التطبيق اختبر اعتماد طلب تجريبي بمبلغ يساوي الفاتورة، ثم اختبر إعادة النقر أو إعادة الطلب للتأكد من عدم إنشاء سند ثانٍ. اختبر أيضاً مبلغاً يتجاوز الرصيد المستحق للتأكد من rollback الكامل.
