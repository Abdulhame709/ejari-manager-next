# دليل تفعيل الإشعارات البرمجية

## الوضع المنفذ حالياً

يوفر النظام الآن من صفحة **الفواتير** وصفحة **سندات القبض** نافذة عربية لمراجعة رقم المستأجر ونص الإشعار، ثم فتح WhatsApp أو تطبيق SMS محلياً. لا ينفذ هذا المسار إرسالاً صامتاً أو تلقائياً؛ إذ يظل التأكيد النهائي بيد الموظف داخل تطبيق الرسائل. هذه هي الطريقة الأخف والأكثر أماناً للاستخدام الفوري، ولا تحتاج مفتاح API أو كشف بيانات اتصال خارجية.

## خيارات الإرسال البرمجي

| الخيار | ما يقدمه | ما يحتاجه من المالك | التعقيد |
|---|---|---|---|
| الإرسال اليدوي المراجع | فتح WhatsApp أو SMS بالنص الجاهز من النظام، مع مراجعة الموظف قبل الإرسال | لا شيء | منخفض |
| مزود موحد: Twilio | SMS وWhatsApp من واجهة API موحدة، مع إمكانية تتبع حالة الرسالة | حساب Twilio، مرسل SMS، مرسل WhatsApp مسجل، ومفاتيح API | متوسط |
| واجهة WhatsApp Cloud مباشرة | رسائل WhatsApp عبر حساب الأعمال لدى Meta، مع webhooks لحالات التسليم | تطبيق Meta، WhatsApp Business Account، رقم هاتف أعمال، ورمز وصول دائم | متوسط إلى مرتفع |

يوفر Twilio واجهة رسائل برمجية تدعم SMS وWhatsApp، وتُصادَق الطلبات فيها بمفتاح API وسره، ولا يجوز وضع هذه القيم داخل الواجهة أو المستودع العام.[1] كما يتطلب إرسال WhatsApp تحت العلامة التجارية تسجيل رقم مرسل في حساب WhatsApp Business المرتبط بالخدمة.[2]

تسمح واجهة WhatsApp Cloud مباشرة بالإرسال واستقبال حالة الرسائل عبر webhooks؛ ويستلزم التشغيل الدائم رمز وصول مخصصاً لمستخدم نظام، ورقم الهاتف التجاري، وربط حساب WhatsApp Business بالتطبيق.[3] وخارج نافذة خدمة العملاء البالغة 24 ساعة، يجب استخدام قوالب WhatsApp معتمدة، كما يجب أن يكون المستأجر قد وافق على تلقي الرسائل.[4]

> لا تُحفظ مفاتيح API أو رموز الوصول في GitHub أو في متغيرات واجهة Vite. توضع حصراً في مخزن أسرار خادمي، ويكون الاستدعاء من دالة خادمية/Edge Function محمية بصلاحيات النظام.

## تصميم التنفيذ المقترح بعد اختيار المزود

| الطبقة | المسؤولية |
|---|---|
| واجهة إيجاري | يراجع الموظف المستأجر والفاتورة أو السند والنص؛ ثم يطلب الإرسال بإجراء صريح. |
| دالة خادمية محمية | تتحقق من دور المستخدم، تتحقق من صحة الهاتف وحالة السند/الفاتورة، ثم تستدعي المزود باستخدام أسرار مخزنة خارج GitHub. |
| سجل إشعارات | يسجل القناة، نوع المستند، المستلم المقنّع، وقت الطلب، معرف المزود، وحالة القبول/التسليم/الفشل. |
| webhook موثق | يتحقق من توقيع المزود ثم يحدّث حالة التسليم. لا يُنفذ أي webhook بلا تحقق للتوقيع. |

## المطلوب لاختيار التنفيذ الفعلي

يرجى تحديد أحد الخيارين التاليين عند الجاهزية:

| إذا اخترت | أرسل أو جهّز |
|---|---|
| Twilio | رقم الحساب، مفتاح API وسره عبر طريقة آمنة، رقم/خدمة SMS المرسلة، ومعرف مرسل WhatsApp أو قالب الرسالة المعتمد. |
| WhatsApp Cloud من Meta | رقم هاتف الأعمال، معرف رقم الهاتف، معرف حساب الأعمال، رمز مستخدم نظام دائم، واسم القالب المعتمد لإشعارات الفواتير. |

لا تُرسل أي مفتاح أو رمز في GitHub. عند تزويد بيانات الاعتماد بطريقة آمنة، يمكن إكمال الإرسال الحقيقي وسجل التسليم، مع إبقاء الإرسال مقيداً بزر موظف مصرح به فقط.

## المراجع

[1]: https://www.twilio.com/docs/messaging/api "Twilio Messaging API Overview"
[2]: https://www.twilio.com/docs/whatsapp "Twilio WhatsApp Business Platform"
[3]: https://developers.facebook.com/documentation/business-messaging/whatsapp/get-started "Meta WhatsApp Cloud API Get Started"
[4]: https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/send-messages "Meta WhatsApp Service Messages"
