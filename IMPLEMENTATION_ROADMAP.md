# برنامج تنفيذ تحسينات نظام إيجاري الحديث

**المشروع المعتمد:** `Abdulhame709/ejari-manager-next` على فرع `development`  
**الهدف:** استكمال المتطلبات العملية المستفادة من نظام الإيجارات القديم بطريقة متوافقة مع React/TypeScript وSupabase، من دون نسخ تقنيات ASP.NET أو SQL Server القديمة.

## قواعد التنفيذ

> تنفذ الدفعات بالترتيب التالي. يرفع كل تغيير إلى GitHub بعد اجتياز فحص TypeScript وبناء الإنتاج. ولا يُنفذ أي تغيير هيكلي في قاعدة Supabase الإنتاجية أو تكامل خارجي مدفوع/حساس إلا بعد تحقق منفصل وموافقة صريحة عند تلك النقطة.

| الحالة | الدفعة | سبب الأولوية | النطاق |
|---|---|---|---|
| مكتملة | P0: سلامة المقبوضات والتقارير | تأثير محاسبي مباشر | عكس سندات القبض، كشف الحساب، CSV، إصلاح مسار `/shops` |
| قيد التنفيذ | P1: مستندات محاسبية رسمية | جاهزية التشغيل والطباعة | تحسين طباعة الفاتورة وسند القبض لـ A4، بيانات الحالة والرصيد والتوقيعات |
| التالية | P2: دورة تجديد العقد | منع تعديل العقود المنتهية وضمان تاريخ العلاقة | تجديد منظم باستخدام `renewed_from_id`، إنهاء العقد السابق، نسخ البيانات الأساسية |
| التالية | P3: استيراد العملاء | تسريع إدخال البيانات ونقلها | CSV/Excel، قوالب، معاينة، تحقق ومنع التكرار |
| التالية | P4: استيراد العقارات والوحدات | إكمال نقل البيانات الأساسية | استيراد متدرج للعقار ثم الوحدات، ربط واضح وتحقيق قبل الحفظ |
| التالية | P5: سجل تدقيق وتنبيهات | الحوكمة والتشغيل اليومي | صفحة بحث في `audit_log`، عقود قريبة من الانتهاء، فواتير متأخرة، قراءات مطلوبة |
| بعد ذلك | P6: اتساق المخطط والأمن | تقليل أخطاء الإنتاج وتعزيز التحكم | مزامنة التعريفات مع Supabase، مراجعة دوال `SECURITY DEFINER`، حماية كلمات المرور المسرّبة |
| مشروط | P7: الرسائل والنسخ الاحتياطي | يحتاج مزودًا خارجيًا أو سياسة مالك النظام | SMS/WhatsApp بمفاتيح مقدمة من المالك، وسياسة Supabase للنسخ والاسترجاع |

## معايير القبول لكل دفعة

| البند | شرط الإغلاق |
|---|---|
| الوظيفة | تدفق كامل في الواجهة مع حالات تحميل وخطأ وفراغ واضحة |
| الصلاحيات | التحقق في الواجهة، مع الاعتماد النهائي على RLS/RPC عند استخدام Supabase |
| البيانات | لا حذف مالي نهائي ولا إدخال جماعي قبل تحقق ومعاينة |
| الجودة | نجاح `npx tsc --noEmit` و`npm run build` |
| التسليم | توثيق موجز، التزام Git واضح، ورفع إلى `development` |

## حدود تحتاج تدخلاً من المالك

لا يمكن تشغيل إرسال SMS أو WhatsApp فعليًا من دون اختيار مزود الرسائل وتقديم بياناته السرية، كما لا ينبغي أتمتة النسخ الاحتياطي أو الاستعادة أو إعادة تشكيل جداول الإنتاج بلا قرار صريح من مالك قاعدة Supabase. ستنفذ الواجهات والضوابط الممكنة داخل المشروع، ثم يطلب الاعتماد فقط عند هذه الحدود.
