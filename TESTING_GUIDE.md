# اختبار يدوي قبل الإطلاق

> لا يحتوي المشروع على أسماء مستخدمين أو كلمات مرور افتراضية. استخدم حسابات اختبار تنشئها أنت، ولا تضع بيانات الدخول داخل Git.

## اختبار التسجيل من بوابة الدخول

1. اختر **الإدارة والموظفون** ثم أنشئ حسابًا:
   - إذا لم يوجد أي مدير حقيقي، يصبح أول حساب إداري `admin`.
   - الحسابات الإدارية التالية تبدأ بصلاحية `viewer` حتى يحدد المدير دورها.
2. اختر **بوابة المستأجر** وأنشئ حسابًا:
   - يُربط بمستأجر غير مرتبط يحمل البريد نفسه، أو يُنشأ سجل مستأجر جديد.
   - بعد الدخول ينتقل إلى `/tenant`.
3. اختر **حساب زائر** وأنشئ حسابًا:
   - لا يحصل على أي دور وظيفي.
   - بعد الدخول ينتقل إلى `/units`.
4. جرّب الدخول ببريد غير مسجل وتأكد من ظهور رسالة تطلب إنشاء حساب أولًا.
5. تأكد أن حقول البريد وكلمة المرور فارغة ولا توجد بيانات دخول جاهزة.

## حسابات الصلاحيات المطلوبة

من صفحة **المستخدمون والصلاحيات** عيّن حسابًا منفصلًا لكل دور:

1. `admin` — وصول كامل.
2. `manager` — جميع العمليات عدا إدارة المستخدمين.
3. `accountant` — القراءات والفواتير والسندات والتقارير وطلبات الدفع.
4. `data_entry` — الوحدات والمستأجرون والعقود والقراءات، دون حذف.
5. `viewer` — لوحة التحكم والتقارير للقراءة فقط.
6. `tenant` — بوابة المستأجر وبياناته فقط.
7. `visitor` — بوابة الوحدات العامة فقط.

## اختبار الدخول والتوجيه

- سجّل الدخول بكل حساب دون تحديث الصفحة يدويًا.
- تأكد أن الموظف ينتقل مباشرة إلى `/`.
- تأكد أن المستأجر ينتقل مباشرة إلى `/tenant`.
- تأكد أن الزائر ينتقل مباشرة إلى `/units`.
- تأكد أن الحساب الموقوف أو الذي لا يحمل نوعًا/دورًا صالحًا يرى رسالة منع واضحة.
- افتح رابط صفحة غير مسموحة مباشرة وتأكد من إعادة التوجيه إلى الصفحة الرئيسية المناسبة.
- تأكد أن الزائر غير المسجل يستطيع أيضًا فتح `/units` دون حساب.

## اختبار الوظائف

- أنشئ وحدة ثم مستأجرًا ثم عقدًا ثم قراءة ثم فاتورة ثم سند قبض.
- ارفع إثبات دفع من حساب المستأجر واعتمده أو ارفضه من حساب المحاسب/المدير.
- تأكد من ظهور سبب الرفض للمستأجر.
- تأكد أن `data_entry` لا يرى أزرار الحذف.
- تأكد أن `viewer` لا يرى صفحات أو أدوات التعديل.
- اختبر تسجيل الخروج ثم الدخول بحساب آخر دون بقاء صلاحيات الحساب السابق.
