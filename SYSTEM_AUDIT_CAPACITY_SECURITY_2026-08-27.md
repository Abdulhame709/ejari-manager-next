# تقرير التدقيق الشامل: الوظائف والسعة والأمان

**النظام:** إيجاري (EJARI)  
**المستودع المعتمد:** `Abdulhame709/ejari-manager-next` — الفرع `development`  
**بيئة قاعدة البيانات المدققة:** `ejari-rental-system` (`vvogmytwnoyuphrjmemj`)  
**تاريخ التدقيق:** 27 أغسطس 2026  
**المنهج:** فحص قراءة فقط لقاعدة الإنتاج، مراجعة الكود والترحيلات، فحوصات البناء والثوابت المالية، عينة استجابة منخفضة الأثر، ومراجعة توجيهات Supabase وVercel الرسمية. لم يُنشأ فرع ولا بيانات اختبار ولم تُعدّل بيانات التشغيل.

## الملخص التنفيذي

التطبيق الحديث يغطي دورة إدارة الإيجارات الأساسية بصورة جيدة، ويحتوي على ضوابط محاسبية أفضل من النظام القديم في نقاط مهمة؛ منها إنشاء الفواتير من معاملة مركزية، وعكس سند القبض بدلاً من حذفه، وتجديد العقد كعملية موثقة، وسجل تدقيق، وRLS على كل جداول `public`. اجتازت أنواع TypeScript وبناء الإنتاج وفحص الثوابت المالية المخصص للمشروع بنجاح.

مع ذلك، لا يمكن اعتماد النظام حالياً باعتباره **مثبت القدرة تحت ضغط تشغيلي مرتفع**. قاعدة الإنتاج ما زالت شبه فارغة، كما أن لوحات المتابعة والتقارير تجمع بيانات كاملة في المتصفح، و73 سياسة RLS تحتاج تحسيناً يمنع إعادة تقييم دوال المصادقة لكل صف، ويوجد 23 مفتاحاً خارجياً بلا فهرس تغطية. لذلك فالحكم الحالي هو: **جاهز للتشغيل المتدرج منخفض الحجم بعد معالجة الأولويات الحرجة، وغير معتمد بعد لتحمل نمو كبير أو حمل متزامن كثيف.**

> لا تعني استجابة صفحات النشر السريعة أن عمليات الفوترة والتقارير تحت ضغط مستخدمين حقيقيين قد اجتازت اختباراً كاملاً. عينة HTTP تقيس وصول المسار فقط، أما اختبار التحمل المعتمد فيحتاج بيانات مماثلة للإنتاج وحسابات أدوار وبيئة لا تعرّض السجلات المالية للخطر.

| محور التقييم | النتيجة الحالية | الحكم |
|---|---|---|
| البناء والثوابت المالية | TypeScript وبناء الإنتاج وفحص الثوابت المالية نجحت | جيد |
| تدفقات العمل المحاسبية في الكود | توليد فاتورة مركزي، أرشفة آمنة، اعتماد دفع، عكس سند، وتجديد عقد | جيد مع حاجة لاختبار قبول فعلي |
| اختبار المستخدمين الفعليين | غير منجز؛ لا توجد بيانات تشغيلية كافية ولا حسابات أدوار اختبار مخصصة | غير مكتمل |
| السعة وقابلية التوسع | مخاطر معروفة في RLS والفهارس والتقارير ولوحة التحكم | يحتاج معالجة قبل التوسع |
| الأمان | RLS ورؤوس حماية ومفاتيح خدمية خارج البناء إيجابية؛ تحذيرات RLS ودوال عامة وتبعيات معروفة قائمة | متوسط إلى جيد، مع أولويات واضحة |
| النسخ والاستعادة | سياسة مكتوبة؛ التنفيذ الفعلي للنسخ الدوري لم يبدأ على خطة Free | غير مكتمل تشغيلياً |

## 1. نطاق الاختبار المنفذ وما يثبته

نفذ التدقيق فحصاً غير مدمر للمستودع والنشر وقاعدة البيانات. نجح الأمر `npm run typecheck`، ونجح بناء الإنتاج، كما نجح `npm run test:financial`. يغطي اختبار الثوابت وجود إنشاء فاتورة ذري، منع التكرار الشهري، الإقصاء الصحيح للفواتير الملغاة من التقارير، أرشفة السجلات بدلاً من الحذف، ضوابط إقرار طلبات الدفع، واستيراد CSV/Excel بالمعاينة والتحقق. هذا **اختبار بنيوي ووقائي**، وليس اختبار قبول مستخدم نهائي.

تعثر الأمر التجميعي `npm run check` عند فحص التنسيق فقط. السبب ليس خطأ بناء أو نوعاً خاطئاً، بل وجود **1,261 خطأ Prettier و11 تحذيراً** في مستودع المصدر. جميع أخطاء التنسيق قابلة للإصلاح آلياً، لكن لا ينصح بتشغيل تنسيق شامل ورفع تغيير واسع من دون فصل ذلك في التزام مستقل ومراجعته.

قاعدة الإنتاج تحتوي حالياً على 22 جدولاً عاماً كلها مفعّل عليها RLS. توجد 9 ملفات تعريف مستخدمين، وعميل واحد، وسجل تدقيق واحد، وحساب بنكي واحد، بينما لا توجد بعد عقارات أو وحدات أو عقود أو فواتير أو سندات قبض أو قراءات. لذلك نجح فحص البنية، لكنه لا يقدّم عينة حقيقية لسرعة تقارير تحتوي آلاف السجلات.

| الاختبار | النتيجة | حدود الاستنتاج |
|---|---|---|
| TypeScript | ناجح | يثبت توافق الأنواع، لا يثبت صحة كل سيناريو عمل |
| بناء Vite/Nitro | ناجح | يثبت قابلية إصدار حزمة إنتاج |
| ثوابت مالية مخصصة | ناجح | يثبت وجود ضوابط كودية ومحاذير محاسبية محددة |
| فحص Lint الكامل | متعثر بسبب تنسيق 1,261 موضعاً | لا توجد أخطاء ESLint وظيفية مؤكدة ضمن هذا الناتج وحده |
| فحص npm audit | 15 ملاحظة: 2 منخفضة، 5 متوسطة، 8 عالية، ولا حرجة | يحتاج تحديثاً مضبوطاً للتبعيات، لا `audit fix` عشوائياً |
| فحص سجلات قاعدة البيانات خلال 24 ساعة | لا يوجد نمط أخطاء تطبيقية متكرر؛ سجل اتصال واحد أُغلق من الطرف العميل | العينة محدودة جداً بسبب انخفاض النشاط |

## 2. فحص المسارات والنشر

اختبرت عينة منخفضة الأثر تتكون من ثلاث طلبات متتالية لكل من مسارات الدخول والوحدات والمحلات والتقارير والتدقيق. أعادت جميع الطلبات HTTP 200. متوسط زمن استجابة كل مسار في هذه العينة كان بين 224 و294 مللي ثانية، وهو مناسب لاستجابة صفحة منشورة في وقت الفحص، مع ملاحظة أن هذا لا يتضمن زمن الاستعلامات بعد تسجيل الدخول أو زمن تنزيل ملفات كبيرة أو ضغطاً متزامناً.

| المسار | الطلبات | متوسط زمن الاستجابة | الاستجابات غير 200 |
|---|---:|---:|---:|
| `/login` | 3 | 254 ms | 0 |
| `/units` | 3 | 294 ms | 0 |
| `/shops` | 3 | 257 ms | 0 |
| `/reports` | 3 | 234 ms | 0 |
| `/audit` | 3 | 224 ms | 0 |

تحققت رؤوس الحماية المنشورة من وجود CSP، وHSTS لمدة سنتين مع `includeSubDomains` و`preload`، ومنع التضمين عبر `X-Frame-Options: DENY`، ومنع استنتاج النوع عبر `X-Content-Type-Options: nosniff`، وسياسة مراجع مقيدة، وتعطيل الكاميرا والميكروفون والموقع في `Permissions-Policy`. هذه طبقة حماية مهمة للواجهة، لكنها لا تغني عن صحة RLS أو تحديث التبعيات.

## 3. الفجوات الوظيفية المتبقية

أنجز المشروع خصائص كان تدقيق النظام القديم قد صنفها ذات أولوية: عكس سندات القبض، كشف الحساب والتصدير CSV، الطباعة الرسمية، تجديد العقد، الاستيراد المنظم، التنبيهات، وسجل التدقيق. الإرسال يظل يدوياً بطلب من المالك، لذلك لا تعد عدم إضافة مزود SMS أو WhatsApp فجوة في النطاق المتفق عليه.

الفجوات التالية لا تمنع بدء الإدخال الأولي منخفض الحجم، لكنها تمنع وصف التطبيق بأنه مكتمل مؤسسياً أو قابل للحوكمة الكاملة من دون متابعة.

| الأولوية | الفجوة | الأثر | الإجراء المقترح |
|---|---|---|---|
| P0 | لا توجد اختبارات قبول تلقائية بالأدوار الحقيقية، ولا اختبارات RLS قابلة للتكرار | لا يمكن إثبات أن كل دور يمكنه/لا يمكنه تنفيذ كل عملية بعد تغيير لاحق | إضافة pgTAP لسياسات RLS وPlaywright لمسارات الدخول، العميل، العقد، الفاتورة، السند، وعكس السند |
| P0 | لا توجد بيانات تشغيلية كافية لتأكيد سلوك التوليد والتقارير والقبض فعلياً | لا يمكن تأكيد الاستعلامات والنماذج والطباعة مع بيانات حقيقية | تنفيذ اختبار قبول يدوي منظم بعد إدخال أول دفعة بيانات صحيحة، مع سجل نتائج وليس بيانات وهمية |
| P1 | التصدير الحالي CSV؛ لا يوجد ملف Excel أصلي أو PDF مولد للتقارير المجمعة | بعض الجهات تحتاج تنسيق Excel أو PDF رسمي متعدد الصفحات | إضافة XLSX من مصدر بيانات التقرير نفسه، ثم PDF حسب قالب معتمد |
| P1 | إدارة مرفقات العقود والعملاء ليست دورة وثائق مكتملة بإصدارات وسياسة احتفاظ | خطر فقد وثائق أو خلط نسخ العقود | إضافة Storage خاص، روابط موقعة، سجل رفع، تصنيفات، وفترة احتفاظ |
| P1 | لا توجد طباعة دفعية منظمة لمجموعة فواتير | جهد يدوي عند طباعة فواتير شهر كامل | إضافة قائمة اختيار وطباعة/تصدير دفعي بحجم صفحات مضبوط |
| P2 | سجل الدخول والنشاط لا يقدّم لوحة حدث مصادقة مستقلة | تقل جودة التحقيق في مشكلات الدخول | عرض آخر دخول والأحداث الحساسة في صفحة المستخدم، مع سياسة خصوصية واضحة |
| P2 | لا توجد مراقبة أعطال تطبيقية مركزية | قد لا يعرف المالك عن خطأ متكرر قبل بلاغ المستخدم | ربط خدمة مراقبة أخطاء بعد اختيار المالك ومفتاحه السري |

## 4. تقييم السعة وتحمل الضغط

### 4.1 ما يدعم السعة حالياً

الفوترة الشهرية تنفذ من دالة قاعدة بيانات مركزية، وتستخدم قفلاً استشارياً لكل فترة محاسبية مع فحص تكرار على مستوى الوحدة والشهر والسنة. هذا يمنع تنافس المستخدمين على إنشاء نفس الفواتير مرتين، وهو تصميم صحيح للسلامة عند وجود عدة موظفين. كما أن تقرير المقبوضات يتجاهل السندات الملغاة والعكسية، وتُسجل العمليات الحساسة في `audit_log`.

التطبيق يجزئ صفحات الواجهة، والنشر يعيد المسارات المدققة بنجاح. مكوّن `xlsx` موجود كحزمة منفصلة بحجم يقارب 857 كيلوبايت في مخرجات الخادم، وهو لا يمنع التشغيل لكنه نقطة يجب مراقبتها على الهواتف ذات الشبكات الضعيفة عند استخدام الاستيراد والتصدير.

### 4.2 ما يحد القدرة عند النمو

لوحة التحكم تشغّل 11 استعلاماً متوازياً عند فتحها، ومنها عدة `count: exact` واستعلامات تجلب كل الوحدات النشطة وكل قراءات الشهر وتحسب القراءات الناقصة في المتصفح. كما تجمع التقارير أرصدة العملاء وكشف الحساب والفواتير والسندات في الواجهة من دون ترقيم صفحات أو حد افتراضي للصفوف. هذا مناسب جداً للعدد الحالي، لكنه يصبح مكلفاً كلما زاد عدد العقود والفواتير والقراءات.

دالة الفوترة المركزية تعمل بحلقة على كل عقد نشط، وداخل كل دورة تنفذ عمليات بحث للقراءة وأنواع العدادات والرصيد السابق. هذا يحافظ على الوضوح والصحة، لكنه يجعل زمن الإصدار ينمو مع عدد العقود النشطة. قبل توسيع قاعدة البيانات، ينبغي قياس الدالة ببيانات مماثلة للحجم المتوقع، ثم نقل أجزاء متكررة إلى استعلامات جماعية أو إضافة الفهارس اللازمة إذا ظهرت خطط تنفيذ بطيئة.

وجد مستشار الأداء في Supabase 116 ملاحظة: **73 تحذيراً** من إعادة تقييم دوال المصادقة أو الدوال المساعدة داخل RLS لكل صف، و**18 تحذيراً** من وجود سياسات permissive متعددة، و**23 ملاحظة** عن مفاتيح خارجية بلا فهارس تغطية، وملاحظتان عن فهارس غير مستخدمة. توثق Supabase أن تغليف `auth.uid()` أو الدالة الثابتة لكل استعلام داخل `select` يسمح للمخطط بتشغيلها مرة واحدة بدلاً من كل صف، وأن فهرسة أعمدة RLS والمفاتيح الخارجية تحسن الأداء عند نمو الجداول. [1] [2]

| مستوى الاستخدام | الحكم الحالي | متطلبات الانتقال إلى المستوى التالي |
|---|---|---|
| إدخال أولي وتجربة تشغيل بعدد قليل من المستخدمين والوحدات | مناسب بحذر | إكمال اختبار قبول يدوي ونسخة احتياطية يومية موثقة |
| نمو مستمر في العقود والفواتير والقراءات | غير معتمد بعد | تطبيق فهارس الأداء بعد مراجعة، إصلاح RLS، وإضافة ترقيم صفحات وتجميعات SQL |
| ضغط متزامن متكرر في نهاية الشهر أو توليد فواتير كبير | غير مختبر وغير معتمد | قياس استعلامات على بيانات مماثلة للحجم، اختبار تحميل خارج الإنتاج، وقياس زمن التوليد وقفل الفترة |
| نمو مؤسسي كبير أو متطلبات SLA | يحتاج تصميم تشغيلي إضافي | مراقبة، حدود طلبات، تنبيهات، اختبار تحميل دوري، وخطة قدرة مرتبطة بخطة Supabase وVercel |

لا يوصى بتنفيذ ضغط اصطناعي على الإنتاج، لأن هذا قد يؤثر في المستخدمين أو يغير كلفة الموارد ولا يمثل سيناريو واقعياً في قاعدة تكاد تكون فارغة. الاختبار الصحيح ينفذ على بيئة بيانات تماثلية أو بعد توفر بيانات فعلية كافية وبموافقة على نافذة اختبار.

## 5. تقييم الأمان والموثوقية

### عناصر إيجابية

جميع جداول `public` المدرجة في الإنتاج مفعّل عليها RLS، ووجدت سياسات وصول لكل جدول. لا يظهر مفتاح `SUPABASE_SERVICE_ROLE_KEY` أو أي معرف خدمة مماثل داخل حزمة المتصفح المبنية؛ وجوده محصور في عميل خادمي منفصل يقرأه من متغيرات البيئة. منطق الواجهة يفشل بصورة آمنة عند تعذر تحميل الملف أو الدور خلال عشر ثوانٍ؛ إذ يلغي الدور والوصول بدلاً من منح صلاحية جزئية. كما أن عكس السند وتجديد العقد واعتماد طلب الدفع تستخدم دوال معاملات مع قيود دور وسجل تدقيق.

توثق Supabase أن RLS يجب أن يبقى مفعلاً على كل جدول في مخطط مكشوف، وأن صلاحيات الجداول والسياسات يجب أن تضبط معاً، لأن وجود سياسة وحده لا يلغي المنح الافتراضية. [3] هذا متحقق جزئياً في النظام من خلال RLS والسياسات، لكن يجب اختباره آلياً لكل جدول وكل دور قبل إعلان اكتمال الأمن.

### المخاطر المفتوحة

| الأولوية | النتيجة | مستوى الخطر | المعالجة المطلوبة |
|---|---|---|---|
| P0 | 8 ثغرات عالية و5 متوسطة في تبعيات npm، منها حزمة TanStack Start Server Core | متوسط إلى مرتفع | مراجعة التحديث المتوافق في فرع منفصل، تشغيل اختبارات البناء والثوابت، ثم نشر تدريجي؛ لا ينصح بـ `npm audit fix --force` |
| P0 | لا توجد اختبارات pgTAP تؤكد سماح/منع RLS لكل دور | مرتفع من ناحية الإثبات | إنشاء اختبارات deny/allow للـ anon والمستخدمين والأدوار الحساسة قبل تعديلات مخطط أخرى |
| P1 | دالتا طلب حساب الموظف والمستأجر متاحتان لـ anon بوصفهما SECURITY DEFINER | متوسط | هذا مقصود لتسجيل الطلبات، لكنه يحتاج rate limiting وCAPTCHA أو تحققاً مضاداً للإساءة قبل زيادة الاستخدام العام |
| P1 | 17 دالة SECURITY DEFINER متاحة للمستخدمين المسجلين | متوسط | راجع كل دالة للتأكد من فحص الدور قبل القراءة/الكتابة، ولا تسحب التنفيذ من الدوال اللازمة للتشغيل قبل بديل مدروس |
| P1 | حماية كلمات المرور المسرّبة غير مفعلة | متوسط | هذه الميزة تتطلب Supabase Pro؛ البديل الحالي هو قاعدة كلمات مرور محلية من 8 أحرف وحرف ورقم |
| P1 | 18 مجموعات من سياسات RLS permissive متعددة | متوسط | راجعها حسب الدور والعملية، وادمجها فقط عندما تكون دلالتها المتوقعة OR غير مطلوبة |
| P1 | سجل الترحيلات في الإنتاج لا يطابق كل ملفات الترحيل المحلية | متوسط | أنشئ ترحيل baseline موثقاً بعد مقارنة المخطط؛ لا تنفذ ملفات قديمة، خصوصاً ملف `demo_open_access`، بشكل أعمى |
| P2 | لا توجد مراقبة أعطال خارج سجلات Supabase/Vercel | منخفض إلى متوسط | إضافة مراقبة أحداث وتدفق تنبيه بعد اختيار المزود |

يحذر مستشار Supabase من أن عدة سياسات permissive على العملية نفسها تعمل بمنطق OR وقد توسع الوصول أو ترفع كلفة التحقق. [2] لا تثبت هذه الملاحظة وجود تسريب في النظام، لكنها تستحق اختباراً دورياً لأنها تصعّب تدقيق الصلاحيات عند نمو القواعد.

## 6. خطة المعالجة بالترتيب

تبدأ المعالجة بالتأكد من الصحة والأمان، ثم تهيئة الأداء، ثم تحسينات التشغيل. لا تطبق أي ترحيلات على الإنتاج ضمن هذه الخطة من دون مراجعة أثرها وموافقة المالك.

| الترتيب | المهمة | النتيجة القابلة للقياس | يحتاج تغيير إنتاجي؟ |
|---:|---|---|---|
| 1 | تحديث تبعيات npm المتأثرة ومراجعة التغييرات | `npm audit` بلا ثغرات عالية قابلة للمعالجة | نعم، نشر كود بعد اختبار |
| 2 | إضافة اختبارات RLS وPlaywright لمسارات الدور | مصفوفة سماح/منع قابلة للتكرار ونتيجة CI | لا في البداية؛ قد يتطلب تجهيز CI |
| 3 | إنشاء ترحيل أداء جديد ومراجع | فهارس مفاتيح خارجية ذات صلة، وتحسين RLS بـ `(select auth.uid())` | نعم، DDL يتطلب موافقة |
| 4 | تحويل لوحة التحكم والتقارير إلى تجميعات SQL وترقيم صفحات | انخفاض عدد الصفوف المنقولة والاستعلامات لكل صفحة | نعم، كود وربما RPC |
| 5 | قياس توليد الفواتير بخطة تنفيذ وبيانات مماثلة للحجم | زمن إصدار موثق لكل حجم، مع تحسين مسار O(n) إن لزم | لا على الإنتاج قبل توافر بيئة قياس |
| 6 | تفعيل نسخ يومي خارجي واختبار استعادة ربع سنوي | دليل نسخة ناجحة وRTO فعلي | يحتاج وصول مالك وإجراء تشغيلي |
| 7 | توحيد سجل الترحيلات وإزالة الاعتماد على ملفات قديمة | مصدر حقيقة واحد للمخطط | نعم، بعد مراجعة دقيقة |

## 7. سياسة النسخ الاحتياطي والاستعادة

تم إعداد سياسة تشغيلية مستقلة في الملف `BACKUP_RESTORE_RETENTION_POLICY.md`. تعتمد السياسة، مع بقاء الخطة الحالية Free، نسخة منطقية يومية لقاعدة البيانات ونسخة مستقلة لكائنات Storage ورفعاً مشفراً خارج Supabase. تستهدف السياسة احتفاظاً بـ35 نسخة يومية و12 شهرية و7 سنوية، وRPO مستهدفاً قدره 24 ساعة وRTO قدره 4–6 ساعات حسب الأصل المستعاد.

توضح Supabase أن النسخ اليومية المدارة متاحة في الخطط المدفوعة، وأن Free يحتاج نسخاً منطقية منتظمة خارج الموقع. كما أن Storage objects لا تدخل ضمن نسخة قاعدة البيانات؛ لهذا تتضمن السياسة ملف فهرسة وتجزئة لكل مرفق. [4] كما تتضمن مساراً منفصلاً للتراجع عن نشر Vercel المعيب؛ إذ يعيد Vercel توجيه النطاق إلى نشر سابق لكنه لا يعيد متغيرات البيئة تلقائياً. [5]

## 8. القرار التشغيلي المقترح

يستطيع المالك بدء إدخال البيانات الفعلية بصورة متدرجة بعد تنفيذ النسخة اليومية الأولى وتوثيقها، لكن يوصى بعدم إطلاق حمل كبير أو إدخال عدد كبير من العقود دفعة واحدة قبل معالجة P0 وإعداد اختبارات قبول. لا توجد علامة على عطل نشط في مسارات النشر المدققة أو في بنية المعاملات المحاسبية، لكن غياب بيانات فعلية يعني أن الضمان التشغيلي تحت الضغط غير مكتمل، لا أنه ناجح.

المسار العملي الأنسب هو معالجة تبعيات npm أولاً، ثم بناء اختبارات RLS والقبول، ثم تقديم ترحيل أداء واحد متحكم فيه للمراجعة قبل التطبيق. بعد أن تحتوي القاعدة على حجم بيانات مناسب، ينفذ قياس أداء حقيقي على استعلامات لوحة التحكم والتقارير والفوترة، مع تحديد حدود السعة بناءً على أرقام فعلية لا تقديرات.

## المراجع

[1]: https://supabase.com/docs/guides/troubleshooting/rls-performance-and-best-practices-Z5Jjwv "Supabase — RLS Performance and Best Practices"
[2]: https://supabase.com/docs/guides/database/database-advisors "Supabase — Performance and Security Advisors"
[3]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase — Row Level Security"
[4]: https://supabase.com/docs/guides/platform/backups "Supabase — Database Backups"
[5]: https://vercel.com/docs/instant-rollback "Vercel — Performing an Instant Rollback on a Deployment"
