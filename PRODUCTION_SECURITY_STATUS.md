# حالة أمان بيئة الإنتاج

**قاعدة البيانات الفعلية:** `ejari-rental-system` (`vvogmytwnoyuphrjmemj`)  
**تاريخ المراجعة:** 26 أغسطس 2026

## نتيجة المراجعة

تمت مزامنة تعريفات TypeScript من مخطط Supabase الفعلي، ثم تصحيح استدعاءات RPC لتتطابق مع معاملات الدوال الاختيارية. هذه المزامنة تقلل خطر أن تعتمد الواجهة على حقول أو توقيعات قديمة عند التطوير اللاحق.

| المجال | الوضع | القرار التشغيلي |
|---|---|---|
| `reverse_receipt` و`renew_contract` | دوال `SECURITY DEFINER` متاحة لـ `authenticated` فقط وتتحقق داخلياً من صلاحيات المدير/المدير التنفيذي | مقبول ومقصود؛ لا تُسحب صلاحية التنفيذ لأنها مطلوبة لتدفق الأعمال، ويستمر التحقق الداخلي وسجل التدقيق. |
| أرشفة العملاء والوحدات والعقود وإدارة المستخدمين | دوال إدارية متاحة للمستخدمين المسجلين فقط، مع سحب صلاحية `anon` في ترحيل الحماية | مقبول بشرط استمرار التحقق من الدور داخل كل دالة قبل أي تغيير. |
| طلب حساب مستأجر أو موظف | دالتان عامتان متاحتان للزوار عمداً حتى قبل تسجيل الدخول | مقصود لتشغيل طلبات فتح الحساب. تتحقق الدوال من الحقول المطلوبة وتعيد الطلب المعلق نفسه عند تكرار البريد الإلكتروني. |
| حماية كلمات المرور المسرّبة | تمت محاولة التفعيل من لوحة Supabase بعد موافقة المالك، لكن الحفظ رُفض لأن الميزة متاحة لخطة Supabase Pro فأعلى | لا يمكن تفعيلها عبر SQL أو الواجهة العامة على الخطة الحالية. يمكن الترقية لتفعيلها، أو اعتماد سياسة بديلة لكلمات المرور بعد موافقة المالك. |

## تنبيه مهم عن تقرير Supabase

> يعرض مدقق Supabase تحذيراً لكل دالة `SECURITY DEFINER` متاحة عبر واجهة RPC. التحذير لا يثبت وحده وجود خلل؛ فبعض هذه الدوال تحتاج الصلاحية لتنفيذ عمليات ذرية محمية. لذلك لا ينبغي تنفيذ `REVOKE EXECUTE` بصورة جماعية، لأنه سيعطل وظائف مثل اعتماد الدفع، الأرشفة، تجديد العقود وعكس السندات.

## نتيجة محاولة تفعيل الحماية

تم فتح صفحة **Authentication → Sign In / Providers → Email** وتفعيل خيار **Prevent use of leaked passwords** ثم حفظه بعد موافقة المالك. أرجعت Supabase رسالة صريحة بأن الحماية المعتمدة على HaveIBeenPwned متاحة لخطة **Pro فأعلى**، ولذلك لم يُحفظ التغيير على الخطة الحالية.

| المسار | النتيجة | الأثر |
|---|---|---|
| الترقية إلى Supabase Pro أو أعلى | يمكن بعدها إعادة تفعيل الحماية نفسها | يضيف رفض كلمات المرور المعروفة أو سهلة التخمين عند التسجيل وتغيير كلمة المرور. |
| بديل من الخطة الحالية | يتطلب موافقة منفصلة لرفع الحد الأدنى لكلمة المرور وتحديد متطلبات التعقيد | يقلل ضعف كلمات المرور، لكنه لا يطابق فحص قاعدة كلمات المرور المسرّبة. |

## معايير استمرار الحماية

| المعيار | التطبيق في المشروع |
|---|---|
| لا صلاحيات مجهولة للعمليات المالية أو الإدارية | سحب `anon` من الدوال الإدارية، مع بقاء طلبات فتح الحساب العامة فقط. |
| لا حذف محاسبي نهائي | الأرشفة وعكس السندات يحفظان التاريخ والمرجع وسجل التدقيق. |
| الواجهة ليست مصدر السلطة | حماية المسارات لتحسين التجربة، وRLS/RPC هي الجهة النهائية للتحقق. |
| اتساق المخطط | `src/integrations/supabase/types.ts` مولد من بيئة الإنتاج ويمثل مصدر الأنواع الحالي. |
